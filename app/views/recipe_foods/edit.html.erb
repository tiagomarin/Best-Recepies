<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing recipe food</h1>

  <%= render "form", recipe_food: @recipe_food %>

  <%= link_to "Show this recipe food", user_recipe_recipe_food_path(current_user, @recipe, @recipe_food), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to recipe foods", user_recipe_recipe_foods_path(current_user, @recipe), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
